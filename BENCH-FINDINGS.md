# Benchmark Execution Findings

Purpose: record every problem encountered during execution (lib bugs/friction, wrong or insufficient agent docs, plan inaccuracies, tooling friction) with solutions, so a final phase can plan lib fixes and agent-doc updates.

## [P0.1] medium — plan guessed dual react-store 0.9.3 was stale residue; it is a real react-router pin
- Problem: The plan's Version Snapshot said `@tanstack/react-store` 0.9.3 in `pnpm-lock.yaml` is "stale residue to investigate and consolidate" (because react-form 1.33.5 depends on `^0.11.0`). Investigation showed 0.9.3 is actively required: `@tanstack/react-router@1.168.25` declares a hard (non-peer) dependency `"@tanstack/react-store": "^0.9.3"`, and semver caret on 0.x minors cannot resolve to 0.11.x. The just-released `@tanstack/react-router@1.170.32` (latest) still declares `^0.9.3`, so the dual resolution persists even after the Phase 0.2 router bump. `@tanstack/store` is duplicated the same way (0.9.3 pulled by react-store 0.9.3, 0.11.1 by form-core/react-store 0.11.1).
- Context: `pnpm why -r @tanstack/react-store`; `pnpm view @tanstack/react-router@1.168.25 dependencies --json`; `pnpm view @tanstack/react-router@1.170.32 dependencies --json`; `pnpm-lock.yaml` snapshot entries for `@tanstack/react-store@0.11.1` / `0.9.3` and `@tanstack/store@0.11.1` / `0.9.3`.
- Solution: Left both versions installed per the plan's own fallback rule ("if a real consumer pins it, identify it in the sub-phase report and leave it, do not force"). Dependency outcome for react-form itself: verified latest (1.33.5), no change.
- Follow-up: plan correction

## [P0.1] low — `pnpm why @tanstack/react-store` at repo root prints nothing
- Problem: The plan (and its Validation Criteria) says to run `pnpm why @tanstack/react-store`, but run at the workspace root it exits with zero output because the root project declares no dependency on it; the resolution chains only appear with the recursive flag.
- Context: `pnpm why @tanstack/react-store` (empty) vs `pnpm why -r @tanstack/react-store` (full chains per project).
- Solution: Used `pnpm why -r @tanstack/react-store` for the real dependency picture.
- Follow-up: plan correction

## [P0.1] low — react-router chains appear in every project's `pnpm why` tree via react-form's optional peer
- Problem: `pnpm why -r @tanstack/react-store` shows `@tanstack/react-router 1.168.25` chains inside packages that never import the router (formedible, ai-builder, ai-picker, ui, builder), which reads like a dependency leak at first glance.
- Context: `@tanstack/react-form@1.33.5` declares `@tanstack/react-start` in `peerDependenciesMeta` as optional; because `apps/web` installs react-start, pnpm resolves a peer-suffixed react-form instance (`react-form@1.33.5(@tanstack/react-start@...)` in `pnpm-lock.yaml` line 4496) that drags the react-start → react-router → react-store 0.9.3 chain into the shared resolution graph.
- Solution: No action needed; interpreted the trees as resolution-graph reachability, not per-project imports. Future phases reading `pnpm why` output should not mistake these chains for package-level dependencies.
- Follow-up: agent-docs update

## [P0.1-val] none — no problems encountered
- Problem: None. All gates passed on first run (`pnpm install` exit 0 clean, `pnpm run check-types` 7/7, `pnpm run build` 6/6), and every implementer claim was independently reproduced: react-form/form-core latest is 1.33.5, the seven declarers all carry `^1.27.1`, `@tanstack/react-router@1.168.25` and latest `1.170.32` both hard-depend on `@tanstack/react-store@^0.9.3` (so the dual react-store/store 0.9.3+0.11.1 lockfile entries are live requirements, correctly left in place per the plan fallback), plain `pnpm why @tanstack/react-store` at the root is empty, and the full Version Snapshot table still matches execution-day `pnpm view` output for every package.
- Context: Validator re-ran all gate commands and registry queries itself; working tree contained only this findings file as untracked (no source/manifest/lockfile edits).
- Solution: N/A — validation passed with no friction beyond what the implementer already logged.
- Follow-up: none

## [PS] high — docs/api page claims collapseLabel/expandLabel are never read, but the hook reads both
- Problem: `/docs/api` (apps/web/src/routes/docs/api.tsx, UseFormedibleOptions section bullet and two table rows) states "collapseLabel and expandLabel exist in the options type, but the hook source does not read them" / "Typed compatibility label. The current hook source does not read it." The current `packages/formedible/src/hooks/use-formedible.tsx` DOES read them (CollapsibleSection `collapseLabel={resolveDynamicText(config.collapseLabel ?? 'Collapse', values)}` and the expandLabel sibling) — they are the collapse/expand toggle labels for `section.collapsible` groups.
- Context: Found while writing the formedible usage skill; verified against source before documenting. Consumers following the API page would believe the labels are dead config and hand-roll section toggles instead.
- Solution: Skill documents the real behavior (hook-level collapse/expand labels for collapsible sections, defaults 'Collapse'/'Expand').
- Follow-up: agent-docs update

## [PS] high — docs describe onFormComplete firing before formOptions.onSubmit; source order is reversed
- Problem: `/docs/analytics` page snippet ("Page and submit tracking") shows `analytics.trackFormComplete(value)` running BEFORE `await config.formOptions.onSubmit?.(...)`, and `/docs/api` analytics table says onFormComplete "Runs before formOptions.onSubmit". In `use-formedible.tsx` the submit path awaits the consumer's onSubmit FIRST, then calls `analytics.trackFormComplete` and `trackSubmissionPerformance` (consumer errors skip completion tracking and keep the draft in storage).
- Context: Order matters to consumers who rely on abandonment/completion semantics: with the documented order a throwing onSubmit would still count as a completed form.
- Solution: Skill documents the source order (onSubmit -> complete/performance analytics -> reset unless `resetOnSubmitSuccess: false` -> clear persistence key).
- Follow-up: agent-docs update

## [PS] medium — sliderConfig.showTooltip used by examples but neither typed nor consumed
- Problem: `apps/web/src/features/docs/compatibility-examples.tsx` (advanced-field-types-form) and `tests/compatibility-examples/core-examples.ts` set `sliderConfig: { ..., showTooltip: true }`. `FormedibleSliderConfig` in `packages/formedible/src/lib/formedible/types.ts` has no `showTooltip` key, and `slider-field.tsx` never reads it — it passes through the `[customProp: string]: unknown` index signature silently.
- Context: Example config that type-checks while doing nothing; anyone copying the docs example inherits a dead key.
- Solution: Skill omits showTooltip and documents the typed sliderConfig keys that are real (marks, gradientColors, valueMapping, visualizationComponent, valueLabelPrefix/Suffix, valueDisplayPrecision, showValue, showRawValue).
- Follow-up: lib fix (either implement the tooltip or drop the key from examples)

## [PS] medium — arrayConfig runtime keys (itemLabel, addButtonLabel, removeButtonLabel, itemPlaceholder) are consumed but untyped
- Problem: `array-field.tsx` reads `arrayConfig.itemLabel`, `.addButtonLabel`, `.removeButtonLabel`, and `.itemPlaceholder`, but `FormedibleArrayConfig` does not declare them; they type-check only via the `customProp` index signature. The docs fields page tells users to use them, yet a typo (e.g. `addItemLabel`) fails silently.
- Context: Same silent-passthrough class as the sliderConfig finding — the index signature makes unknown keys indistinguishable from supported ones.
- Solution: Skill documents the four keys with an explicit "accepted at runtime via custom-prop passthrough, not named in FormedibleArrayConfig" caveat.
- Follow-up: lib fix (type them in FormedibleArrayConfig)

## [PS] low — README says fields and formOptions.defaultValues are "required by the type", but both are optional
- Problem: `packages/formedible/README.md` ("Basic use") states "`fields` and `formOptions.defaultValues` are required by the type". In `types.ts`, `UseFormedibleOptions.fields?` and `FormedibleFormOptions.defaultValues?` are both optional (fields defaults to [], defaultValues falls back to `{}`).
- Context: Minor docs inaccuracy; harmless at runtime but misleading for typing expectations.
- Solution: Skill describes the real optionality (fields defaults to an empty list; defaultValues falls back to an empty object).
- Follow-up: agent-docs update

## [PS] low — no usable version signal for consumers (package 0.0.0 private, parser exports version '0.1.0')
- Problem: `@formedible/formedible` package.json is `"version": "0.0.0"` and private (consumers install the `formedible-core` registry item, which carries no version), while `formedible-parser` exports `version = '0.1.0'`. There is no consistent version a consumer (or this skill) can pin behavior against.
- Context: The skill stamps branch (`re-codex`) + date (2026-08-31) + package version instead; a later lib change cannot be version-detected downstream.
- Solution: Skill states its provenance explicitly in the opening section.
- Follow-up: tooling

## [PS] low — customProp index signature hides unsupported keys across field configs
- Problem: `FormedibleFieldConfig`, nested config objects (slider/array/etc.), `UseFormedibleOptions`, and analytics/persistence configs all end with `[customProp: string]: unknown`, so unsupported or misspelled keys (showWordCount at top level, precision, showTooltip, itemLabel typos) compile without warning and silently no-op. The types file documents a few of these in comments, but the compiler cannot catch them.
- Context: Root cause of the two medium findings above; confusing API surface for consumers and for agents writing configs.
- Solution: Skill's verification bar requires every option key used to exist in `FormedibleFieldConfig` or the matching nested config type; the arrayConfig keys are called out explicitly.
- Follow-up: lib fix (narrow the index signatures or add branded excess-property checks)

## [PS-val] high — skill gotcha claims page navigation is not rendered when tabs are configured; source renders both
- Problem: `/home/didi/.agents/skills/formedible/SKILL.md` gotcha "When both tabs and pages are configured, tab filtering wins; page navigation is not rendered" is half false. Field filtering does check tabs first (`use-formedible.tsx` renderFields, hasConfiguredTabs branch), but `FormNavigation` renders whenever `hasConfiguredPages` is true (`use-formedible.tsx` line ~1036: `{runtime.hasConfiguredPages ? (<FormNavigation .../>) : ...}`) with no tabs guard, and `form-navigation.tsx` unconditionally renders the Previous/Next/Submit buttons. A mixed tabs+pages config renders the tab bar AND page navigation.
- Context: Validator grep of docs and tests found no source for the "not rendered" clause — `api.tsx` only says "active tab filtering wins over page filtering", so the clause looks invented rather than mis-copied.
- Solution: Correct the gotcha to "tab filtering wins for field rendering; page navigation still renders when any field has `page` (or `pages` is set), so avoid mixing tabs and pages".
- Follow-up: skill fix (Phase S revision)

## [PS-val] medium — parser reference claims the runtime's four alias normalizations are parser-accepted; parser rejects two of them
- Problem: `references/parser-and-ai.md` says accepted aliases include "the same four aliases the runtime normalizes" (multicombobox → multiCombobox, maskedInput → masked implied). `formedible-parser.ts` fieldTypeAliases (lines ~125-137) has no `multicombobox` or `maskedInput` entries, `multiCombobox` is absent from `supportedFieldTypes` entirely, and `sanitizeField` (line ~808) rejects any type not in `supportedFieldTypeSet` when `fieldTypeValidation` is true (default). Generated configs using those aliases are rejected, not normalized.
- Context: Validator read the alias map and supported-type list directly; the parser also accepts extra aliases the skill omits (`number-input`, `date-picker`, `file-upload`, `telephone`).
- Solution: Restrict the sentence to the aliases actually in the parser map and optionally list the parser-only extras.
- Follow-up: skill fix (Phase S revision)

## [PS-val] medium — skill's array example ships dead objectConfig keys, including one that is not typed for arrays
- Problem: `references/fields.md` array example sets `arrayConfig.objectConfig: { collapsible: true, defaultExpanded: true, showCard: true, ... }`. `array-field.tsx` reads only `objectConfig.fields/layout/columns` (lines ~108-110); `collapsible`/`showCard`/`defaultExpanded`/`defaultCollapsed` are never read in the array path (`showCard` only in `object-field.tsx`, `defaultCollapsed` nowhere). Worse, `FormedibleArrayObjectConfig` (types.ts ~71-80) declares `defaultCollapsed`, not `defaultExpanded`, so the example key only compiles via the `[customProp: string]` index signature — the exact silent-passthrough trap the implementer logged as `[PS]` findings, contradicting the skill's own verification bar ("no invented props", "every option key used exists in ... the matching nested config type").
- Context: Found during validator accuracy sweep; the skill flags the passthrough caveat for itemLabel/addButtonLabel/removeButtonLabel but not for these objectConfig keys.
- Solution: Drop `collapsible`/`defaultExpanded`/`showCard` from the array example (keep layout/columns/fields) or annotate them as typed-but-unread in the array renderer.
- Follow-up: skill fix (Phase S revision)

## [PS-val] low — minor accuracy/completeness gaps in the skill
- Problem: (a) `references/flows.md` dynamic-text location list omits `help.text`/`help.tooltip`, which the hook does resolve (`resolveFieldHelp` in use-formedible.tsx); (b) `references/fields.md` value table implies date stores `string` while `date-field.tsx` stores a `Date` object on user change (`field.onChange(nextValue ? nextDate : undefined)`), normalizing to `YYYY-MM-DD` only for display — a real typing gotcha for zod consumers; (c) `references/validation.md` async-pass order omits the field-local schema step that actually runs first (validation.ts onChangeAsync); (d) hook-level `fieldClassName`/`labelClassName` (types.ts, applied in renderField) are undocumented in the skill.
- Context: Validator completeness sweep against docs site (llms.txt mentions "hints" for dynamic text) and source reads.
- Solution: Extend the two lists and the date row; optionally add fieldClassName/labelClassName to the flows reference.
- Follow-up: skill fix (Phase S revision)

## [PS-fix] medium — fixed seven dogfooding inaccuracies in the formedible skill (tabs/pages gotcha, parser aliases, array objectConfig, help tokens, date storage, async order, hook-level classes)
- Problem: (1) SKILL.md gotcha claimed page navigation "is not rendered" when tabs and pages are both configured — false: `FormNavigation` renders whenever `hasConfiguredPages` is true (use-formedible.tsx ~1036; form-navigation.tsx renders Previous/Next/Submit unconditionally) and only FIELD filtering prefers tabs (renderFields ~738); a mixed config renders the tab bar AND page buttons, with page clicks changing `currentPage` but never the visible fields. (2) parser-and-ai.md claimed the parser accepts "the same four aliases the runtime normalizes" — the parser's `fieldTypeAliases` (formedible-parser.ts ~124-138) has no `multicombobox`/`maskedInput`, `multiCombobox` (and `combobox`) are absent from `supportedFieldTypes`, and `sanitizeField` (~808) rejects them with `UNSUPPORTED_FIELD_TYPE`; conversely the parser accepts aliases the skill omitted (`number-input`, `date-picker`, `file-upload`, `telephone`, `color-picker`, `color`, `checkboxGroup`). (3) fields.md array example shipped dead `arrayConfig.objectConfig` keys (`collapsible`/`defaultExpanded`/`showCard`) — array-field.tsx reads only `fields`/`layout`/`columns` (~108-110); `showCard` is consumed only by object-field.tsx (~69), and `defaultCollapsed` (the key actually typed on `FormedibleArrayObjectConfig`, types.ts ~74) is read nowhere. (4a) dynamic-text token-target lists omitted `help` (plain string) plus `help.text`/`help.tooltip`, all resolved in `resolveFieldHelp` (use-formedible.tsx ~540-548). (4b) the value table implied date stores `string` — date-field.tsx (~74) stores a local-midnight `Date` on user change and `undefined` when cleared; `'YYYY-MM-DD'` is display-only, so a `z.string()` schema breaks the moment a user picks a date. (4c) validation.md async-pass order omitted the field-local schema step that runs FIRST (validation.ts onChangeAsync ~595). (4d) hook-level `fieldClassName`/`labelClassName` (types.ts ~586-589, merged in renderField ~646 via tailwind-merge) were undocumented.
- Context: Dogfooding validation (Phase S) flagged these against branch `re-codex`; every changed claim was re-verified in `packages/formedible/src` and `packages/formedible-parser/src` before rewording. Adjacent source observations recorded while fixing (not skill defects, unfixed): `ParserConfig.fieldTypeValidation` only changes `generateSystemPrompt` wording — `sanitizeField`'s type rejection is unconditional; and the parser's `supportedFieldTypeInfo` suggests `z.string().datetime()` for `date` while the runtime stores `Date`.
- Solution: Rewrote the SKILL.md tabs/pages gotcha and token list; corrected parser-and-ai.md to the parser's real 13-entry alias set plus the runtime-only-alias rejection gotcha (including the inverted color canonicalization); stripped dead keys from the fields.md array example and documented which objectConfig keys are consumed where; fixed the date value row, example `Values` types, and added the zod `z.date()`/`z.coerce.date()` gotcha; completed the dynamic-text target list in SKILL.md and flows.md; corrected the async pass order in validation.md; documented `fieldClassName`/`labelClassName` in flows.md (Form surface) and fields.md (common keys).
- Follow-up: none

## [PS-val2] none — no problems encountered
- Problem: None. All three attempt-1 failures and all four minor items re-verified against source and confirmed fixed: (1) SKILL.md:91 tabs/pages gotcha now matches `use-formedible.tsx` renderFields (tab-first field filter), `hasConfiguredPages` (:350), and the FormNavigation render (:1036; form-navigation.tsx renders the buttons unconditionally); flows.md:67 aligned. (2) parser-and-ai.md:55 lists exactly the 13 entries of `fieldTypeAliases` (formedible-parser.ts ~124-137); "normalized in every parse path" verified via `validateAndSanitize:1016-1017` running `normalizeAiGeneratedConfig` first for parse/parseStructured/parseAiOutput; the :56 rejection claims verified — combobox/multiCombobox absent from `supportedFieldTypes`, multicombobox/maskedInput unmapped, `sanitizeField:808-810` throws the exact code `UNSUPPORTED_FIELD_TYPE` unconditionally; color-inversion direction correct. (3) fields.md array example now uses only fields/layout/columns (all read by array-field.tsx:108-110); the new paragraph's claims verified — bordered item row (:113), collapsible/showCard/defaultExpanded/collapseLabel/expandLabel consumed only by object-field.tsx (:19-20, :61, :69), `defaultCollapsed` read nowhere. Minors: help tokens match `resolveFieldHelp` (use-formedible.tsx ~540-550); date row/Values/zod gotcha match date-field.tsx:66-75 (local-midnight Date on pick, undefined on clear); validation.md:23 async order matches validation.ts:594-619; fieldClassName/labelClassName match types.ts:586-589 + the renderField merge via `cn` (twMerge, lib/utils.ts).
- Context: Re-validation (attempt 2), read-only apart from this entry; fences balanced, no TODO/any slop, `git status --short` shows only this findings file untracked. [PS-fix]'s two adjacent source observations also spot-checked true (`fieldTypeValidation` appears only in parser-config-schema.ts incl. generateSystemPrompt:290, never gating sanitizeField; supportedFieldTypeInfo suggests `z.string().datetime()` for date at formedible-parser.ts:73 while the runtime stores Date).
- Solution: N/A — no new inaccuracies found in the edited sections. One non-blocking wording nit: parser-and-ai.md:56 "the first two are aliases the runtime normalizes" has an ambiguous antecedent ('multiCombobox' is the runtime canonical type; 'multicombobox'/'maskedInput' are the aliases); every stated library behavior is correct under either reading.
- Follow-up: none

## [P0.2] low — router-generator bump silently reorders routeTree.gen.ts declarations (pure permutation)
- Problem: After bumping the router/start cluster, `pnpm run build:web` regenerated `apps/web/src/routeTree.gen.ts` with 134 insertions / 134 deletions. Every hunk is a reordering: the new `router-generator@1.167.33` sorts route imports/declarations differently (index route first, then by path segment) than the version that produced the committed file. Verified pure permutation (sorted added lines == sorted removed lines, byte-identical); route set unchanged (17 routes), no structural/type changes.
- Context: `git diff apps/web/src/routeTree.gen.ts`; sorted-lines comparison of the diff. The file carries `@ts-nocheck` so typecheck could not have caught semantic drift — the route-tree test (`pnpm --dir apps/web run test:route-tree`, 2/2 pass) is the real guard and still passes.
- Solution: Regenerated via the package's normal build (`pnpm run build:web`, which runs the tanstackStart vite plugin), never hand-patched, exactly as the plan's §0.2 note anticipated. No `apps/web/src` source fixes were needed for the bump.
- Follow-up: none

## [P0.2-val] none — no problems encountered
- Problem: None. Every implementer claim was independently reproduced. Changed-file scope exactly as allowed (`apps/web/package.json`, `pnpm-lock.yaml`, `apps/web/src/routeTree.gen.ts` + this findings file; no `packages/*` touched). The manifest diff changes exactly the four `@tanstack` ranges with caret style kept, and each new range equals the execution-day `pnpm view` latest (react-query 5.102.8, react-router 1.170.32, react-router-devtools 1.167.1, react-start 1.168.49 — nothing newer on the table). `pnpm install` exit 0; `pnpm install --resolution-only` re-ran resolution and printed zero unmet-peer warnings (only the pre-existing `node-domexception@1.0.0` deprecation, present in the HEAD lockfile). Gates: `check-types:web` exit 0, `build:web` exit 0 (prerender complete), `check-types` 7/7, `test:route-tree` 2/2. Permutation claim mechanically proven: `git show HEAD:apps/web/src/routeTree.gen.ts | sort` vs `sort apps/web/src/routeTree.gen.ts` are byte-identical (437 lines both, 36 `path:` entries both, 18 `as any` both), and rebuilding via `pnpm run build:web` reproduced the identical 134/134 diff — the file is genuine tooling output (router-generator 1.167.33 sort change), not hand-patched; the `as any` tokens are the generator's own pre-existing output under `@ts-nocheck`. Transitive coherence: each of router-core (1.171.27), router-plugin (1.168.35), router-generator (1.167.33), router-utils (1.162.2), start-plugin-core (1.171.39), start-client-core (1.170.27), start-server-core (1.169.31), history (1.162.1), virtual-file-routes (1.162.0), query-core (5.102.8) resolves to exactly one version in `pnpm-lock.yaml` (peer-suffixed entries are same-version instances, not drift). Dual react-store 0.9.3+0.11.1 persists as pre-logged: `@tanstack/react-router@1.170.32` still hard-depends on `@tanstack/react-store@^0.9.3` (verified via `pnpm view`), correctly left per the [P0.1] fallback.
- Context: Validator re-ran all gate commands and registry queries itself; working tree contained only the four expected entries. `pnpm install --resolution-only` (pnpm 10's dedicated peer-issue surfacing mode) confirmed the zero-peer-warning claim without mutating the lockfile (post-run `pnpm install` still reports "Lockfile is up to date").
- Solution: N/A — validation passed on first attempt.
- Follow-up: none

## [P0.3] high — plan wrong: chat() dropped top-level temperature/maxTokens in 0.52; sampling moved into per-provider modelOptions
- Problem: The plan's Version Snapshot says "The `chat()` core options our code uses survive in 0.52: `adapter`, `messages`, `systemPrompts`, `temperature`, `maxTokens`, `modelOptions`, `abortController`". Verified against the installed `.d.ts`: `TextActivityOptions` (`@tanstack/ai@0.52.0` `dist/esm/activities/chat/index.d.ts`) has NO `temperature`/`maxTokens` (0.16.0's same file had them at lines 40/44; 0.52 removed them). Because our code spreads `createTanStackChatParameters()` into the `chat()` literal, TypeScript would NOT have flagged the stale keys (spread excess properties are not checked) — they would have been silently dropped, losing temperature/max-token behavior with no compile error.
- Context: `@tanstack/ai-openai@0.22.3` `text/text-provider-options.d.ts` (`OpenAISamplingOptions.temperature`, `max_output_tokens`), `@tanstack/ai-anthropic@0.18.3` `text/text-provider-options.d.ts` (`AnthropicSamplingOptions.temperature`, `max_tokens`), `@tanstack/ai-openrouter@0.19.5` `text/text-provider-options.d.ts` (`Pick<ChatRequest, 'temperature' | 'maxCompletionTokens' | ...>`). 0.16 engine merged top-level params into adapter options (`activities/chat/index.js:201-205`), so moving them into provider-native modelOptions keys is behavior-preserving per provider.
- Solution: `createTanStackModelOptions` (`packages/ai-builder/src/lib/formedible/ai-adapters.ts`) now builds the full per-provider options object — openai `{temperature?, max_output_tokens?}`, openrouter `{temperature?, maxCompletionTokens?}`, anthropic `{temperature?, max_tokens?}` plus the existing thinking config (temperature still omitted while Anthropic extended thinking is enabled). `createTanStackChatParameters` (`ai-generation.ts`) reduced to `{ modelOptions }`; the two tests pinning the old top-level shape were migrated to the new contract (same invariants: no temperature under thinking, thinking budget shape, spread-call regex pins).
- Follow-up: plan correction

## [P0.3] medium — OpenRouter default model id drifted: minimax/minimax-2.7 no longer in the 0.19.5 catalog
- Problem: `DEFAULT_TANSTACK_AI_MODELS.openrouter` was `minimax/minimax-2.7`; the `OPENROUTER_CHAT_MODELS` literal list in `@tanstack/ai-openrouter@0.19.5` `model-meta.d.ts` has zero occurrences of that id — MiniMax ids are `minimax/minimax-m2` … `minimax/minimax-m3` (the "m" generation naming). The type cast at our adapter boundary would have masked it (model is `string` cast to the catalog union), so nothing but inspection/tests would catch it.
- Context: `grep -o "minimax/minimax-2\.7"` over `model-meta.d.ts` → 0 matches; `minimax/minimax-m2.7` present (also `:free` variant). OpenAI `gpt-5.4-mini` and Anthropic `claude-sonnet-4-6` both verified still present in their 0.22.3/0.18.3 catalogs (no drift).
- Solution: Default changed to the closest current equivalent `minimax/minimax-m2.7`; `ai-model-catalog.test.ts` OpenRouter fetch fixture updated from the retired id to `minimax/minimax-m2.7` (fixture data only — fakes still injected, no network).
- Follow-up: none

## [P0.3] medium — 0.52 RUN_FINISHED usage is a TokenUsage record; cached/reasoning counts moved into details sub-objects
- Problem: All three provider adapters in the new stack emit `RUN_FINISHED.usage` as a `TokenUsage` record (`{promptTokens, completionTokens, totalTokens, promptTokensDetails?, completionTokensDetails?}` — `buildBaseUsage`/`buildResponsesUsage`/`buildAnthropicUsage`/`buildOpenRouterUsage`), with cached tokens under `promptTokensDetails.cachedTokens` and reasoning tokens under `completionTokensDetails.reasoningTokens`. Our `readUsage` only read the flat AG-UI spec names (`cachedInputTokens`/`reasoningTokens` top-level), so those two `AiUsageMetadata` fields would silently degrade to `undefined` under 0.52. (Usage only appears as the AG-UI spec array after `normalizeStreamChunk`/`toSpecTokenUsage`, which the in-process chat() stream does not apply to yielded chunks — verified the engine only uses `rebuildTokenUsage` for middleware hooks.)
- Context: `@tanstack/ai@0.52.0` `utilities/ag-ui-usage.d.ts` (`SpecTokenUsage` flat names vs `TokenUsageLeftover`), `@tanstack/ai-event-client@0.11.2` `index.d.ts:190-227` (TokenUsage), adapter `usage.js` files.
- Solution: `readUsage` (`ai-generation.ts`) now also reads `promptTokensDetails.cachedTokens` and `completionTokensDetails.reasoningTokens` as fallbacks behind the existing flat-name reads; public `AiUsageMetadata` shape unchanged. Test fixture at ai-builder.test.ts:850 already used the canonical record shape (`usage: { promptTokens, completionTokens, totalTokens }`), so no fake-stream changes were required.
- Follow-up: none

## [P0.3] low — conversationId grep gate needs a domain-vs-option distinction
- Problem: The gate "`grep -rn conversationId packages/ai-builder/src/lib/formedible/` shows no deprecated usages" cannot be read as "zero grep hits": `conversationId` legitimately remains as our OWN persisted domain concept in `GeneratedFormSnapshot.conversationId` (`ai-types.ts:193`) and throughout `ai-storage.ts` (stored-data schema + runtime validation of persisted snapshots). Renaming those would break stored conversation data and is unrelated to the TanStack deprecation. The deprecated chat() option usage is gone: `ai-generation.ts` now passes `threadId: request.threadId`, and our `AiGenerationRequest.conversationId` field — which existed solely to feed the chat() option — was renamed to `threadId` (doc comment states it is passed to chat() as the AG-UI threadId).
- Context: `@tanstack/ai@0.52.0` `types.d.ts:847-856` deprecates `conversationId` in favor of `threadId` ("When conversationId is omitted, the runtime falls back to threadId automatically" — so passing the same value as `threadId` preserves correlation); 0.16 also accepted `threadId`. UI component prop `conversationId` (chat-interface/ai-builder/sidebar) intentionally keeps its name — it is the app's conversation id, forwarded as `threadId` at the request construction site.
- Solution: Deprecated-option usage fully migrated (owner + quick-synced ui copies); domain occurrences documented here for the phase-wide validator.
- Follow-up: agent-docs update

## [P0.3] low — new THINKING_TEXT_MESSAGE_CONTENT event family added to the normalizer
- Problem: `@ag-ui/core` 0.0.49 (via `@tanstack/ai@0.52.0` EventType enum, `client.d.ts:28-51`) adds a `THINKING_TEXT_MESSAGE_CONTENT` sibling family next to `REASONING_MESSAGE_CONTENT`. None of our three providers emits it today (Anthropic 0.18.3 emits `REASONING_MESSAGE_CONTENT` — `adapters/text.js:633`), and it is not part of the typed `StreamChunk`/`AGUIEvent` union, but a consumer emitting it would have degraded to a raw event in our normalizer.
- Context: `normalizeStreamChunk` from 0.52 (`utilities/normalize-stream-chunk.d.ts`: `(chunk: AdapterYieldChunk) => Array<StreamChunk>`) was evaluated per the plan and NOT adopted: it is a spec-key-stripping/metadata-folding utility returning an array (splits `TOOL_CALL_END.result` into a synthetic `TOOL_CALL_RESULT`, folds non-spec keys into `metadata.tanstack`), not a semantic event classifier — adopting it would add an array-flattening layer without simplifying our `unknown → AiStreamEvent` mapping or changing our public shape. Our normalizer kept; every branch re-verified against 0.52 chunk shapes (`TEXT_MESSAGE_CONTENT`, `REASONING_MESSAGE_CONTENT`, `TOOL_CALL_*` incl. the `toolName` alias on `TOOL_CALL_START`, `RUN_ERROR`, `RUN_FINISHED` with `finishReason: 'stop'|'length'|'content_filter'|'tool_calls'|null`).
- Solution: Added `THINKING_TEXT_MESSAGE_CONTENT` to the thinking-delta branch; STEP_FINISHED branch kept (the 0.18.3 Anthropic adapter still emits STEP_FINISHED chunks carrying a string `delta`).
- Follow-up: none

## [P0.3] low — pre-existing (not a 0.52 regression): Anthropic adapter double-emits each thinking delta, so accumulated thinking text duplicates on real streams
- Problem: Both the old (`@tanstack/ai-anthropic@0.8.8` `adapters/text.js:487-494`) and new (`0.18.3` `adapters/text.js:632-644`) Anthropic adapters yield TWO chunks per `thinking_delta`: a `REASONING_MESSAGE_CONTENT` with the delta string, then a `STEP_FINISHED` carrying the SAME delta string (plus `content: accumulatedThinking`). Our normalizer maps both to `thinking-delta` events, so `accumulator.thinking` receives each thinking token twice on a real Anthropic stream. The behavior tests never caught it because their fakes use distinct content for the two chunk types (ai-builder.test.ts:865-866). UNRESOLVED — unchanged by this migration (byte-identical emission pattern in both versions); fixing it (e.g. ignore STEP_FINISHED thinking deltas when a REASONING_MESSAGE_CONTENT stream is active, or prefer `content` accumulation over `delta`) is a behavior change beyond 0.3's "verify branches still match" scope.
- Context: Verified by reading both adapters' `processAnthropicStream`-equivalent blocks side by side.
- Solution: UNRESOLVED (pre-existing, intentionally left behavior-identical for the dependency bump).
- Follow-up: lib fix

## [P0.3] low — pnpm 10 ignores @openrouter/sdk postinstall (harmless no-op)
- Problem: `pnpm install` printed the "Ignored build scripts: @openrouter/sdk" warning. The package's postinstall is `node scripts/check-types.js || true` — it can never fail the install and produces nothing the runtime needs, so the ignored script is cosmetic (no `pnpm approve-builds` required).
- Context: `node_modules/.pnpm/@openrouter+sdk@0.13.20/.../package.json` scripts block. Also noted: a stale `@openrouter+sdk@0.12.14` directory remains under `node_modules/.pnpm` (unpruned residue); `pnpm-lock.yaml` contains exactly one `@openrouter/sdk` entry (0.13.20).
- Solution: No action; documented so later phases do not mistake the warning for an install problem.
- Follow-up: none

## [P0.3-val] high — model-id migration incomplete: dead `minimax/minimax-2.7` survives in every RUNTIME-facing default while only the test-facing constant was fixed (VERDICT: FAIL)

- Problem: 0.3 changed `DEFAULT_TANSTACK_AI_MODELS.openrouter` to the catalog-valid `minimax/minimax-m2.7` (`packages/ai-builder/src/lib/formedible/ai-adapters.ts:20` + synced ui copy) — but that constant has NO runtime consumer in-repo (only `index.ts` exports and tests reference it). The declarations that actually seed requests still carry `minimax/minimax-2.7`, an id that does not exist in EITHER the old `@tanstack/ai-openrouter@0.8.5` or the new `0.19.5` typed catalogs (both have always had `minimax/minimax-m2.7`; verified `grep -o '"minimax/..."' model-meta.d.ts` on both installs — so the pre-0.3 default was already invalid, i.e. a pre-existing typo, and the implementer's "old id gone from catalog" framing reads as version drift when the id was never valid). Result of the partial fix: the repo now ships TWO contradictory OpenRouter defaults, and the runtime one is the dead id, silently accepted by the `as unknown as OpenRouterAdapterModel` double cast and pinned green by tests asserting the stale string. Stale sites: `packages/ai-builder/src/components/formedible/ai/provider-selection.tsx:13` (feeds `createDefaultProviderSettings` → every ai-builder chat request), `packages/ai-builder/src/lib/default-picker-schema.ts:6` (feeds `ai-picker-utils.ts` picker defaults), owner `packages/ai-picker/src/lib/default-picker-schema.ts:6`, synced `packages/ui/src/components/formedible/ai-picker/lib/default-picker-schema.ts:6` and `packages/ui/src/components/formedible/ai/provider-selection.tsx:13`, and `apps/web/src/routes/docs/ai-builder.tsx:151` (docs page's own providerOptions). 0.3 ALSO introduced fresh docs drift: `apps/web/src/routes/docs/ai-builder.tsx:96` (bullet), `:106` (table row), and `:117` (embedded `DEFAULT_TANSTACK_AI_MODELS` snippet) now state `minimax/minimax-2.7` for a constant whose real value the same PR changed to `m2.7`. Tests pinning the dead id that must be updated with the fix: `packages/ai-picker/src/ai-picker.test.ts:53,118,323,333,636`; `packages/ai-builder/src/components/formedible/ai/ai-builder.test.ts:41,331,491,592,606,1086,1536`; `packages/ai-builder/src/lib/formedible/ai-storage.test.ts:81,259,272`.
- Context: Validator catalog proof: new `@tanstack/ai-openrouter@0.19.5` `model-meta.js` contains `minimax/minimax-m2.7` (2 hits incl. `:free`) and 0 hits for `minimax/minimax-2.7`; old `0.8.5` `model-meta.d.ts` minimax list is identical in naming (`minimax-01, m1, m2, m2-her, m2.1, m2.5, m2.7`). OpenAI `gpt-5.4-mini` and Anthropic `claude-sonnet-4-6` verified present in their 0.22.3/0.18.3 catalogs (no drift). Runtime default flow: `provider-selection.tsx` `providerOptions[].defaultModel` → `createDefaultProviderSettings()` → `AiGenerationRequest.providerSettings.model` → `createOpenRouterText(model, ...)`; `DEFAULT_TANSTACK_AI_MODELS` is never on that path. Fix discipline: edit owners (`provider-selection.tsx`, `default-picker-schema.ts` in ai-builder; `default-picker-schema.ts` in ai-picker; docs route), then `node scripts/quick-sync.js`, then update the pinning assertions (they assert the corrected default, not a loosened test).
- Solution: UNRESOLVED — validation FAILED on this item (everything else in 0.3 passed: manifests, lockfile, code migration (a)-(f), test migration, sync idempotency, all gates). Fixer: propagate `minimax/minimax-m2.7` to the six runtime default declarations + three docs-route mentions + eleven test assertions, re-run quick-sync and the four gates.
- Follow-up: fixer loop required before Phase 0 phase-wide validation

## [P0.3-fix] high — OpenRouter default `minimax/minimax-m2.7` propagated to all runtime defaults, docs, pinning tests, and registry artifact

- Problem: The 0.3 correction of `DEFAULT_TANSTACK_AI_MODELS.openrouter` (ai-adapters.ts:20) was incomplete: every declaration that actually seeds runtime requests still carried the dead id `minimax/minimax-2.7` — `providerOptions` in `packages/ai-builder/src/components/formedible/ai/provider-selection.tsx:13` (feeds `createDefaultProviderSettings` → every OpenRouter chat request), the picker defaults `packages/ai-builder/src/lib/default-picker-schema.ts:6` and owner `packages/ai-picker/src/lib/default-picker-schema.ts:6`, plus their synced `packages/ui` copies; the docs route stated the dead id in four places; and pinning tests asserted the stale string, keeping it green.
- Context: Sync directions confirmed from `scripts/quick-sync.js`: `packages/ai-picker → packages/ai-builder/src` (registry, copy-only) and `packages/ai-builder → packages/ui/src/components`, `packages/ai-picker → packages/ui/src/components` (registry targets), so owners were edited and `node scripts/quick-sync.js` (run twice, idempotent) regenerated the packages/ui copies and the ai-builder picker-schema copy. Sweep beyond the mandated ts/tsx scope found the same dead id in `packages/ai-builder/public/r/ai-builder.json` (shadcn registry payload embedding stale pre-0.3 source; `build:registry` is NOT part of the turbo build, so the build gate would never refresh it) — targeted replacement applied there rather than a full regen, and in the present-tense default-models statement in `skills/formedible/references/parser-and-ai.md:66`. Historical records mentioning the dead id were left untouched on purpose: prior BENCH-FINDINGS entries, `.review/session-20260819/review-report-15.md`, and `PERF-BENCHMARK-PLAN.md:46`.
- Solution: Owners fixed (`provider-selection.tsx:13`, both `default-picker-schema.ts:6`), docs route `apps/web/src/routes/docs/ai-builder.tsx:96,106,117,151`, all listed pinning assertions migrated not loosened (`ai-picker.test.ts:53,118,323,333,636`; `ai-builder.test.ts:41,331,491,592,606,1086,1536` incl. the regex `/minimax\/minimax-m2\.7/`; `ai-storage.test.ts:81,259,272` with fixture label aligned to catalog naming `MiniMax M2.7`), quick-sync run, plus registry artifact and skill reference. Gates: `pnpm run check-types` 7/7, `pnpm run build` 6/6, `pnpm --filter @formedible/ai-builder run test` 91 pass / 0 fail, `pnpm --filter @formedible/ai-picker run test` 70 pass / 0 fail; mandated grep (`minimax/minimax-2.7` in *.ts/*.tsx over packages+apps, excl. node_modules/dist) returns 0 hits.
- Follow-up: `packages/ai-builder/public/r/ai-builder.json` still embeds other pre-0.3 source drift (e.g. old `dangerouslyAllowBrowser` adapter calls); regenerate it with `pnpm --filter @formedible/ai-builder run build:registry` at the next release/registry step rather than mid-fix. Consider wiring `build:registry` into the release pipeline so registry payloads cannot drift from owners again.

## [P0.3-val2] high — attempt-1 failure fully fixed, BUT registry payload `public/r/ai-builder.json` still embeds pre-0.3 code for all 4 migrated files (VERDICT: FAIL)

- Problem: Every item of the attempt-1 failure is verified fixed: repo-wide dead-id sweep (`*.ts/*.tsx/*.md/*.json` over packages/apps/tests/docs/skills) returns 0 hits; all runtime defaults now `minimax/minimax-m2.7` (provider-selection.tsx owner + ui copy, both default-picker-schema.ts owners + ui copy, ai-adapters.ts constant); docs route's 4 mentions fixed; skill reference fixed; all pinning test assertions migrated 1:1 (ai-picker.test.ts:53,118,323,333,636; ai-builder.test.ts:41,331,491,592,606,1086 + the :1536 regex which was additionally STRENGTHENED — the previously unescaped `2.7` dot is now `2\.7`; ai-storage.test.ts:81,259-260 incl. label `MiniMax M2.7`); ui copies differ from owners only by quick-sync import rewrites (diff-verified line by line); quick-sync re-run idempotent (git status unchanged); gates re-run green (check-types 7/7, build 6/6, ai-builder 91/91, ai-picker 70/70, 0 skipped); manifests/lockfile/migration files untouched since attempt 1. REMAINING DEFECT: `packages/ai-builder/public/r/ai-builder.json` embeds 23 files and 4 of them — `src/components/formedible/ai/chat-interface.tsx`, `src/lib/formedible/ai-adapters.ts`, `src/lib/formedible/ai-generation.ts`, `src/lib/formedible/ai-types.ts` — are still the PRE-migration 0.16-era code (mechanical comparison of every `files[].content` against `packages/ai-builder/<path>`: 18 SAME, 4 DRIFT). The embedded ai-generation.ts still passes `conversationId`, spreads top-level `temperature`/`maxTokens` (silently dropped by 0.52 — the exact bug 0.3 existed to fix), has no nested usage reads and no THINKING_TEXT_MESSAGE_CONTENT branch; embedded ai-adapters.ts still has the old `createTanStackModelOptions` (returns `undefined`/thinking-only — its string-patched minimax id makes it LOOK current); embedded ai-types.ts still declares `conversationId?`; embedded chat-interface.tsx still passes `conversationId`. The payload declares UNVERSIONED `@tanstack/ai`/`ai-anthropic`/`ai-openai`/`ai-openrouter` dependencies, so a consumer installing the item from formedible.dev today resolves 0.52.0 and runs pre-migration code against it. The fixer DISCLOSED this drift in the [P0.3-fix] follow-up line and deferred regen to "the next release/registry step" — but the hand-patch (2 string replacements) is itself the never-hand-patch-generated-output anti-pattern (plan §0.2 rule), and it masks the staleness from the exact grep (dead-id sweep) that now passes green.
- Context: Payload is generated output: `packages/ai-builder/package.json` `build:registry` = `shadcn build`, reading the 23 paths from `packages/ai-builder/registry.json` and embedding current src verbatim (proof: the 18 untouched entries are byte-identical to src, and the fixer's hand-patched provider-selection content is also byte-identical — the pipeline applies no transforms). Root-cause chain: the 0.3 implementer migrated the 4 source files without regenerating the payload; my attempt-1 sweep missed it (grepped ts/tsx/md but not json — my miss, on record); the fixer found it, string-patched 2 entries, and deferred the rest. Regeneration risk (network-fetched registryDependencies, larger diff) is a real but manageable concern; the acceptance criterion below is mechanical either way.
- Solution: UNRESOLVED — attempt-2 validation FAILED on this single item (nothing else remains from attempt 1). Fixer: run `pnpm --filter @formedible/ai-builder run build:registry` (or, if the CLI cannot run in this environment, hand-replace the 4 stale embedded `content` strings with the verbatim current file contents); acceptance criterion: the 23-entry SAME/DRIFT comparison reports 23/23 SAME plus the four gates re-green. The fixer's suggestion to wire `build:registry` into the release pipeline (or add a freshness test comparing payload contents against src) is endorsed — that is the structural fix for this class of drift.
- Follow-up: fixer loop (registry payload) before Phase 0 phase-wide validation; registry-freshness guard recommended so payload drift can never pass gates silently again

## [P0.3-fix2] high — registry payload regenerated properly via build:registry; 4 stale embedded files refreshed, hand-patch anti-pattern eliminated

- Problem: The attempt-2 residual defect is fixed by regeneration, not hand-patching. `packages/ai-builder/public/r/ai-builder.json` embedded 0.16-era code for exactly the 4 migrated files (`chat-interface.tsx`, `ai-adapters.ts`, `ai-generation.ts`, `ai-types.ts`; baseline mechanical comparison before regen: 19 SAME / 4 DRIFT) while declaring unversioned `@tanstack/ai*` deps that resolve to 0.52 — a registry consumer would have installed 0.52 and run pre-migration code whose top-level `temperature`/`maxTokens` are silently dropped by 0.52 `chat()`. The prior fixer's 2-string minimax patch inside the generated payload (disclosed in [P0.3-fix]) was the never-hand-patch-generated-output anti-pattern and masked the staleness from the dead-id grep.
- Context: Regeneration command used: `pnpm --filter @formedible/ai-builder run build:registry` (script exists in `packages/ai-builder/package.json` as `shadcn build`); it ran clean on the first attempt ("✔ Building registry.", no network fetch errors). Pipeline applies NO transformations — verified: all 19 previously-SAME entries are byte-identical before and after regen, and post-regen every `files[].content` equals `packages/ai-builder/<path>` verbatim. Post-regen verification: 23/23 SAME / 0 DRIFT (node script comparing each of the 23 embedded contents against the disk file). New-code markers confirmed in embedded contents: `ai-generation.ts` has `threadId` (2 occurrences), zero `conversationId`, `THINKING_TEXT_MESSAGE_CONTENT` branch present; `ai-adapters.ts` carries the new provider-aware `createTanStackModelOptions(settings): OpenAITextProviderOptions | AnthropicTextProviderOptions | OpenRouterTextModelOptions` (openai `max_output_tokens` / openrouter `maxCompletionTokens` / anthropic `max_tokens` + thinking config); `chat-interface.tsx` maps its `conversationId` prop to `threadId` at the `AiGenerationRequest` construction site (the prop name legitimately persists as the app's persisted-conversation domain id, per [P0.3] low). Payload diff coherent: vs the pre-regen snapshot exactly the 4 stale entries changed and all meta fields (`$schema`/`name`/`title`/`description`/`type`/`dependencies`/`registryDependencies`) are byte-identical; vs HEAD 5 entries differ — the 4 refreshed plus `provider-selection.tsx`, whose sole delta is the `minimax/minimax-2.7` → `minimax/minimax-m2.7` correction now legitimately sourced from disk src (line-level diff: exactly that one line), replacing the earlier hand-patch. Minimax strings in the regenerated payload: `minimax/minimax-m2.7` × 2 (ai-adapters + provider-selection). Gates re-run green: `pnpm run check-types` 7/7, `pnpm run build` 6/6, `pnpm --filter @formedible/ai-builder run test` 91 pass / 0 fail, `pnpm --filter @formedible/ai-picker run test` 70 pass / 0 fail. With 0.52-compatible code embedded, the unversioned-`@tanstack/ai*`-deps exposure flagged in [P0.3-val2] is neutralized for now, though a future major bump would recreate it until deps are version-bounded.
- Solution: Payload regenerated with the package's own script (`pnpm --filter @formedible/ai-builder run build:registry`); no manual edits to the generated file were made; acceptance criteria 1-4 all verified as above.
- Follow-up: recommendation only (NOT implemented): wire `build:registry` into the release pipeline / turbo build graph for `packages/ai-builder` (and the analogous formedible-parser payload), or add a registry-freshness test that mechanically compares every `files[].content` in `public/r/*.json` against the on-disk src (the exact 23/23 script used here) so payload drift can never pass gates silently; consider version-bounding the `@tanstack/ai*` entries in `registry.json` `dependencies` so the published item cannot resolve a future incompatible major.


## [P0.3-val3] none — no problems encountered

- Problem: None. Attempt-3 validation PASSED; both prior failure clusters are fully resolved and no new issues exist in the delta. (1) Acceptance criterion mechanically reproduced: every `files[].content` in `packages/ai-builder/public/r/ai-builder.json` compared against `packages/ai-builder/<path>` on disk — 23/23 SAME, 0 DRIFT. (2) Migration markers verified inside the embedded contents: ai-generation.ts passes `threadId: request.threadId` (zero `conversationId: request.conversationId`), carries the `THINKING_TEXT_MESSAGE_CONTENT` branch, reads nested `promptTokensDetails`/`completionTokensDetails`, and returns `{ modelOptions: createTanStackModelOptions(settings) }` with the old top-level temperature spread gone; ai-adapters.ts embeds the per-provider options builder (`max_output_tokens` / `maxCompletionTokens` / `max_tokens` + `{ type: 'enabled', budget_tokens }`); ai-types.ts declares `threadId?`; chat-interface.tsx passes `threadId: conversationId`. (3) Payload integrity vs HEAD: all meta fields ($schema/name/title/description/type/dependencies/registryDependencies) byte-identical, 23 entries in both directions with zero added/removed entries and zero path/type/target changes; exactly 5 entries' content changed — the 4 regenerated files plus provider-selection.tsx, whose embedded delta is exactly one line (`minimax-2.7` → `minimax-m2.7`, 181 lines both, line 13); the whole payload diff is 5 insertions/5 deletions (one JSON line per entry — minimal, tool-generated, no reformat churn); `minimax/minimax-m2.7` ×2 and dead-id ×0 in the payload. (4) Gates re-run by the validator: check-types exit 0 (7/7), build 6/6, ai-builder 91/91 pass 0 skipped, ai-picker 70/70 pass 0 skipped. (5) Delta integrity: git status file set identical to attempt 2; every non-registry diff stat identical to attempt 2 (only ai-builder.json grew 4→10 marks, explained by regen replacing the hand-patch); repo-wide dead-id sweep 0 hits (excluding this log and the two intentionally preserved historical references PERF-BENCHMARK-PLAN.md:46 snapshot + .review/session-20260819); `node scripts/quick-sync.js` re-run idempotent (git status unchanged, 27 lines). The [P0.3-fix2] entry's claims all cross-check against the validator's own measurements, including its 19 SAME/4 DRIFT pre-regen baseline (consistent with attempt-2's 18 SAME + 1 hand-patched-SAME / 4 DRIFT).
- Context: Validator re-ran the SAME/DRIFT comparison, marker greps, meta/entry-level JSON diff vs HEAD, all four gates, dead-id sweep, and quick-sync idempotency itself. The fixer's structural follow-up recommendations (wire `build:registry` into the release pipeline or add a registry-freshness test comparing every `public/r/*.json` `files[].content` against disk; version-bound the `@tanstack/ai*` registry deps) remain valid and are recorded in [P0.3-fix2] — recommendation-only, correctly not implemented mid-phase.
- Solution: N/A — Phase 0.3 validation PASSED on attempt 3.
- Follow-up: none

## [P0-wide] medium — consumer-smoke gate transiently red on upstream npm publish race (typescript-eslint@8.69.0 vs visitor-keys propagation)

- Problem: First run of `pnpm run test:consumer-smoke:vite-base` during phase-wide validation failed (exit 1, 0/1) inside the shadcn scaffold step: the freshly created `formedible-smoke` Vite project's `pnpm install --no-frozen-lockfile` aborted with `ERR_PNPM_NO_MATCHING_VERSION — No matching version found for @typescript-eslint/visitor-keys@8.69.0` ("latest release is 8.68.0"). Root cause is an upstream race, not Phase 0: `typescript-eslint@8.69.0` was published to npm at 2026-08-31T17:08:46Z — three minutes before the gate run — while its own dependency `@typescript-eslint/visitor-keys@8.69.0` had not yet propagated to the registry (verified `npm view @typescript-eslint/visitor-keys version` → 8.68.0 at 17:11, and `npm view typescript-eslint version` → 8.69.0). The test hardcodes `pnpm dlx shadcn@latest init --preset b5JNwVXgA --base base --template vite`, whose generated template pins the bleeding-edge eslint toolchain; nothing in the repo controls that version.
- Context: Polled the registry: visitor-keys latest flipped to 8.69.0 at 17:12:41Z (~1 minute later). Immediate retry of the gate passed clean: 1/1, exit 0 (31.97s). Zero Phase 0 changes involved — the failing dependency tree lives entirely inside the throwaway scaffold, and all repo-internal gates (check-types 7/7, build 6/6, all test suites) were green before and after.
- Solution: Retry-after-short-wait is the correct response; treated as environment flake, not a gate failure, with registry evidence recorded here. Verdict unaffected (gate green on completion).
- Follow-up: later phases running consumer-smoke (it is in the recurring gate set) should budget one retry and, on `ERR_PNPM_NO_MATCHING_VERSION` for a package published <15 min ago, wait 1-5 min and re-run before escalating to a fixer loop. Structural option (recommendation only): pin the scaffold's toolchain versions or vendor the template to make the gate hermetic against upstream release windows.

## [P1] medium — plan names `submit-50` in DECISION-3 but Appendix A / forms.ts spec omit it
- Problem: DECISION-3 defines the smoke subset as `mount-50`, `typing-50`, `submit-50`, `parser-medium`, and the orchestrator instruction states "submit-50 is the smoke name for the submit scenario". However Appendix A (Scenario Registry, authoritative) registers only `submit-10` / `submit-100`, and Phase 1 requirement 4 specifies paired zod schemas as "small: 10 fields; large: 100 fields" — nothing produces a 50-field submit scenario.
- Context: PERF-BENCHMARK-PLAN.md DECISION-3 vs Appendix A submit rows; Phase 4's compare criteria also list only `submit-{10,100}`.
- Solution: Added `submit-50` to the scenario registry (`comparable: 'cross'`, `smoke: true`) and made the submit-schema generator size-driven (10/50/100) so all three submit sizes share one deterministic generator. Artifact ids and the smoke run use `submit-50` exactly as DECISION-3 requires.
- Follow-up: plan correction (Appendix A + Phase 4 compare row list should include `submit-50`)

## [P1] medium — importing formedible-parser source from a bench tsconfig needs a dual `@/*` path mapping plus the parser's ambient d.ts
- Problem: `packages/formedible-parser/src/index.ts` imports through its own `@/` alias (`@/lib/formedible/formedible-parser`), while `packages/formedible/src` also maps `@/components|hooks|lib/...`. A single bench tsconfig mapping `@/*` to formedible's src alone cannot typecheck (or run) the parser import. Additionally the parser's `@/components/formedible/lib/types` / `.../parser-types` imports resolve only through the ambient declarations in `packages/formedible-parser/src/registry-dependencies.d.ts`, which is included by the parser's own tsconfig glob but not by a bench program unless listed explicitly.
- Context: `tests/bench/tsconfig.json`; parser index.ts imports; verified at runtime with tsx (multi-location paths resolve) and with `tsc -p tests/bench/tsconfig.json`.
- Solution: `paths: { "@/*": ["packages/formedible/src/*", "packages/formedible-parser/src/*"] }` (no filename collisions exist between the two trees, first match wins) and an explicit include of `packages/formedible-parser/src/registry-dependencies.d.ts`. tsx honors the multi-location mapping at runtime, so the same tsconfig drives both typecheck and execution.
- Follow-up: Phase 2's `tsconfig.main.json` (adapter-main) must repeat this two-location pattern for whichever trees the main adapter imports.

## [P1] medium — formedible skill documents nothing about rendered DOM structure, which DOM-driving harnesses (bench adapters, E2E) must know
- Problem: Dogfooding the skill to build the benchmark adapter required stable selectors to drive the rendered form from outside React (click tab triggers, click array add/remove buttons, count rendered fields, find the form element). The skill covers configuration only; the existence of `data-tabs-trigger`, `data-formedible-array-field`, `data-formedible-array-item`, `data-formedible-section`, and the remove-button `aria-label="Remove N"` convention is discoverable only by reading field/layout source. Selects render a `button[name=...]` trigger (not a native `<select>`), which any field-counting or querySelector logic must account for; radio groups share one `name` across many inputs.
- Context: `packages/formedible/src/components/formedible/layout/form-tabs.tsx`, `fields/array-field.tsx`, `fields/select-field.tsx`, `fields/radio-field.tsx`; the adapter's `getRenderedFieldCount` counts distinct `[name]` attributes inside the form for exactly this reason.
- Solution: Read the source for selectors; adapter uses `[data-tabs-trigger="true"]` (ordered by configured tab order), `[data-formedible-array-field="<name>"]` with text/aria-label button matching, `form [name]` distinct-name counting, and `requestSubmit()` for submits.
- Follow-up: agent-docs update (add a "Driving the rendered form / DOM hooks" section to the formedible skill or lib fix: document the data-* contract)

## [P1] low — skill's zod example uses the deprecated `z.string().email()` form; installed zod v4 wants `z.email()`
- Problem: The skill's canonical contact example validates email with `z.string().email('Enter a valid email')`. The repo's installed zod 4.3.6 marks the string-method form deprecated in favor of top-level `z.email()`. The deprecated form still functions, so nothing breaks, but new consumer code copied from the skill inherits deprecation warnings.
- Context: `skills/formedible/SKILL.md` step 4 example; `tests/bench/lib/scenarios/forms.ts` uses `z.email()` and `z.enum([...])` against zod 4.3.6.
- Solution: Benchmark schemas use the v4 top-level API (`z.email()`, `z.string().min(...)`, `z.number().min(...)`, `z.enum([...])`).
- Follow-up: agent-docs update (skill example)

## [P1] low — skill import map assumes the installed-consumer layout; this monorepo's direct-source imports are not covered
- Problem: The skill's import map only documents `@/components/ui/formedible/...` paths of an app that installed the registry item. Inside this repository the established convention (all jsdom test suites, and now the bench adapter) imports the hook directly from `packages/formedible/src/hooks/use-formedible` with a per-suite tsconfig mapping `@/*` → `packages/formedible/src/*`. An agent following only the skill would not know direct-source usage is supported/expected here.
- Context: `tests/formedible/*.test.tsx` imports; `tests/formedible/tsconfig.basic-fields.json`; `tests/bench/lib/adapter-current.ts`.
- Solution: Followed the repo's per-suite tsconfig convention (bench tsconfig mirrors tsconfig.basic-fields.json with DOM libs, `jsx: react-jsx`, and the `@/*` mapping).
- Follow-up: agent-docs update (skill note on monorepo/direct-source usage)

## [P1-val] low — parser-medium throughput is sensitive to scenario position in the process (cold first-scenario runs ~45% slower)
- Problem: Validator ran `bench:smoke` twice (mandated): medians mount-50 58.42→57.34ms (1.9%), typing-50 11.12→11.24ms (1.1%), submit-50 42.22→40.09ms (5.3%), parser-medium 0.2830→0.2816ms (0.5%) — no scenario swings >50%. But pooling every artifact in `tests/bench/results/runs/` shows parser-medium medians 0.2816–0.2922 across all runs where parser executes after jsdom scenarios, versus 0.4168 in the one `--only parser-medium,mount-10 --runs 2` run where parser was the FIRST scenario in the process (~45% slower despite the 3 in-scenario warmup batches — V8 has not tiered up the parser/zod paths yet). Full-run artifact (all 10 scenarios, `--runs 3`) confirms every registered id executes end-to-end (mount-100 122.15ms, typing-100 23.84ms/op, submit-100 90.10ms).
- Context: `tests/bench/results/runs/2026-08-31T17-32-35.267Z-current.json` (parser-first subset, 0.416812) vs every smoke artifact (parser runs 4th, ~0.28–0.29) and the full run 2026-08-31T17-32-25.651Z (parser runs 10th, 0.291909). Registry order fixes scenario order for `bench`/`bench:smoke`, so current-vs-reference comparisons (Phase 4) compare like with like; only `--only` subset artifacts diverge from that context.
- Solution: No code change required for Phase 1 (smoke/full runs are order-stable). Phase 4's report should either refuse to gate on `--only` artifacts against full-run references, or annotate parser metrics with the scenario position — recorded here so the gate design accounts for it.
- Follow-up: Phase 4 design note (regression gating should use order-stable runs; `--allow-regression` exists for exploratory subsets)

## [P1-rework] high — uncommitted Phase 2 WIP was jsdom-mount-based and could not survive the mandated jsdom deletions
- Problem: The working tree held UNCOMMITTED Phase 2 output built against the superseded plan: `tests/bench/lib/adapter-main.ts` and `tests/bench/lib/run-main.ts` (both import the jsdom `render-client`/`dom-bootstrap`; `run-main.ts` drives `mainAdapter.mountForm` in-process) plus `perf-probe.ts` (jsdom probe, carrying the tree's one pre-existing typecheck error TS4104). Deleting `render-client.ts`/`dom-bootstrap.ts` (mandated) makes `adapter-main.ts` uncompilable under `tsc -p tests/bench/tsconfig.json` (its `lib/**/*.ts` include covers it), so no state of the tree simultaneously satisfies "zero jsdom under `tests/bench`" and a green aggregate typecheck while those files remain.
- Context: `git status` before the rework (untracked `adapter-main.ts`, `run-main.ts`, `perf-probe.ts`, `baseline-worktree.ts`, `formedible-main-bridge.d.ts`, `tsconfig.main.template.json`; modified `artifacts.ts`, `package.json` bench:baseline* scripts). The revised plan Phase 2 reworks BOTH files around the SAME driver + a main browser fixture ("Delete the jsdom/in-process UI mount path from adapter-main.ts"), so the pair was scheduled for rewrite regardless; the coordinator was notified mid-rework before deletion and did not object.
- Solution: Deleted `adapter-main.ts`, `run-main.ts`, `perf-probe.ts` alongside the mandated `render-client.ts`, `dom-bootstrap.ts`, `adapter-current.ts` (the browser driver replaces the current adapter, sanctioned by the rework instruction). KEPT the medium-independent Phase 2 assets untouched: `baseline-worktree.ts`, `tsconfig.main.template.json`, `formedible-main-bridge.d.ts` (preserves the verified main-branch API shapes + D11/D12 divergence notes Phase 2 needs) and the `artifacts.ts` worktree-environment additions. Removed the two dangling `bench:baseline:setup`/`bench:baseline` script lines from root `package.json` (`bench:baseline` referenced the deleted `run-main.ts`); Phase 2 re-adds both when its runner lands.
- Follow-up: Phase 2 rework recreates `run-main.ts`/`adapter-main.ts` from the driver + `fixtures/main-consumer/` pattern; nothing else consumes them today.

## [P1-rework] medium — rolldown-vite (vite 8) string aliases do not extension-resolve aliased TS module paths
- Problem: The fixture's first `vite build` failed with `UNRESOLVED_ENTRY ... No such file or directory` on `@formedible-src/hooks/use-formedible`: a `resolve.alias` string entry replacing the prefix produces an extensionless absolute path (`.../src/hooks/use-formedible`) that rolldown (vite 8.0.10) does not resolve against `resolve.extensions`, and formedible's internal `@/lib/...`/`@/components/...` imports would hit the same wall.
- Context: `pnpm --dir apps/web exec vite build --config tests/bench/fixtures/current-consumer/vite.config.ts` with object-form `resolve.alias`; `apps/web/vite.config.ts` instead uses `resolve: { tsconfigPaths: true }`.
- Solution: Dropped manual aliases; the fixture vite config uses `resolve.tsconfigPaths: true` so module resolution goes through the fixture's OWN `tsconfig.json` `paths` (`@formedible-src/*`, `@/*`, `@bench-scenarios`) with full TypeScript resolution semantics — the same mechanism `apps/web` already uses. Build then succeeds (2143 modules, ~285ms).
- Follow-up: Phase 2's `fixtures/main-consumer/` vite config must use `tsconfigPaths` (its own worktree-aliasing tsconfig), not string aliases, for the same reason.

## [P1-rework] low — bench bundle embeds the string "jsdom" via vendored dependencies, forcing the build output out of `tests/bench`
- Problem: The gate "`grep -rn jsdom tests/bench` returns nothing" flagged the gitignored fixture build output: the production bundle (React/zod/TanStack) contains environment-sniffing vendor code mentioning jsdom, so any `dist/` under `tests/bench` breaks the literal grep even though no bench source touches jsdom.
- Context: `grep -rn jsdom tests/bench` matched only `fixtures/current-consumer/dist/assets/index-*.js` after the source-tree cleanup was complete.
- Solution: The fixture's `build.outDir` moved to the gitignored repo-root `.bench-dist/current-consumer` (`.gitignore` entry added), keeping the bench tree grep-clean while `vite preview` serves it exactly as before.
- Follow-up: `fixtures/minimal-consumer/` and `main-consumer/` (Phases 2-3) should reuse the same `.bench-dist/<name>` output convention.

## [P-rev] high — user override: jsdom rejected as a benchmark medium; plan revised to agent-browser-only UI timing (mid-execution)
- Problem: The plan's DECISION-2 made jsdom in-process timing the primary medium with a smaller agent-browser subset, DECISION-3 defined the smoke subset as jsdom scenarios, and DECISION-6 excluded a `main` browser fixture. Mid-execution (Phase 1 committed plus jsdom-based bench code and run artifacts; `2026-08-31T18-*-*main.json` artifacts included), the user REJECTED jsdom as a source of benchmark values — jsdom numbers are not real-world numbers. Every jsdom-produced UI timing record in `tests/bench/results/runs/` (mount/typing/submit medians, current and main) is medium-invalid and superseded; none may be compared against future browser-collected records.
- Context: User override 2026-08-31 on the plan's primary benchmark medium. Real Chromium via `agent-browser` is the established driver in this repo (`tests/e2e/utils/agent-browser.ts`), and the port-allocation pattern exists (`tests/consumer-smoke/utils/ports.ts`).
- Solution: `PERF-BENCHMARK-PLAN.md` revised: DECISION-2/3/6 rewritten (real Chromium via agent-browser is the PRIMARY and ONLY UI medium — in-page `performance.now()` loops via agent-browser JS evaluation, `--enable react-devtools` + `react renders start/stop --json`, `profiler start/stop`, `vitals --json`; jsdom removed from the bench tree entirely; smoke = `mount-50`/`typing-50`/`submit-50` browser + `parser-medium` node, N=5, full N=25; `main` gets a browser fixture whose vite config aliases main's formedible source AND its pinned runtime deps to the baseline worktree's tree, preserving DECISION-1 isolation — the page-reported `@tanstack/react-form` version is the new isolation assertion, recorded in artifacts; un-aliasable deps become findings, jsdom fallback FORBIDDEN). `memory-500` moves in-browser (Chromium heap APIs, e.g. `performance.memory`/`PerformanceObserver`, no forced-GC assumption, API + caveats recorded as artifact notes); `stream-100chunks` runs inside the browser fixture with react-devtools render counts. Phase 1 reworked: committed vite fixture `tests/bench/fixtures/current-consumer/` (shared scenario definitions, `?scenario=` switch) + driver `tests/bench/utils/agent-browser.ts`, fixtures served with `vite preview` on allocated ports, spawned/stopped in `finally`; Phase 3 changed Parallel → SEQUENTIAL (3.1 then 3.2; both modify `run.ts` and the scenario registry); Appendix A media updated (`jsdom` → `agent-browser (Chromium)`, parser stays `node (no DOM)`); risk #4 replaced (browser wall-time cost, N=5 smoke; instrumentation variance mitigated by an un-instrumented timing path with devtools counts reported separately). Phase 1's jsdom artifacts (`render-client.ts`, `dom-bootstrap.ts`, the jsdom adapter mount path, `perf-probe.ts`) are scheduled for deletion in the rework; `scenarios/forms.ts`, `scenarios/index.ts`, `bench-timing.ts` (stats), `artifacts.ts`, and `adapter-types.ts` are reused.
- Follow-up: plan correction

## [P1-rework-val] low — stale references to deleted adapter files survive in committed bench headers/doc comments; plan Phase 1/2 text still names them
- Problem: The rework deleted `adapter-current.ts` (whole file) and the uncommitted `adapter-main.ts`/`run-main.ts`/`perf-probe.ts`, but (a) `tests/bench/lib/adapter-types.ts:3` header still says the contract is "shared by every implementation adapter (`adapter-current.ts` today, `adapter-main.ts` in Phase 2)" — neither exists; (b) six exported contract symbols now have ZERO consumers anywhere (`BenchAdapter`, `MountedForm`, `RootHandle`, `MountFormOptions`, `MsElapsed`, `BenchSubmitContext`, `BenchPersistenceConfig` — only `BenchImplementation` and the BenchField family are imported); (c) `tests/bench/lib/formedible-main-bridge.d.ts:3` and `tests/bench/lib/report-table.ts:4` reference `adapter-main.ts`/`run-main.ts`, which do not exist until Phase 2 recreates them; (d) PERF-BENCHMARK-PLAN.md Phase 1 Outputs still says "Modify: tests/bench/lib/adapter-current.ts (jsdom mount path removed ...)" and Phase 2 Outputs says "Modify: ... run-main.ts ... adapter-main.ts" — all "modify" targets that were deleted ([P1-rework] high logged the deletions but the plan text was not updated). Appendix A also still omits `submit-50` (pre-existing, follow-up already logged as [P1] medium).
- Context: Validator greps (`grep -rn 'BenchAdapter\|MountedForm\|RootHandle\|MsElapsed\|BenchSubmitContext\|BenchPersistenceConfig' tests/bench` minus the contract file → 0 hits; plan path cross-check → `adapter-current.ts` MISSING while the plan's Phase 1 section names it as modified). The deletions themselves are coherent and were sanctioned ([P1-rework] high): the browser driver replaces the current adapter. `check-types` stays green because the dormant d.ts/template compile standalone.
- Solution: Accepted for Phase 1 (no functional impact; the driver + harness-protocol are the real contract now). Phase 2 should either prune the dead `adapter-types.ts` symbols/refresh its header when it recreates the main-side runner, or repoint the header at the driver; plan text should be corrected in the same pass (Phase 1 "Modify adapter-current.ts" → "Delete adapter-current.ts"; Phase 2 "Modify run-main.ts/adapter-main.ts" → "Create").
- Follow-up: Phase 2 cleanup + plan correction

## [P1-rework-val] low — submit-50 smoke median swings more than the implementer's claimed 8.1% between validator runs (still far under the 50% investigation bar)
- Problem: The implementer reported "max swing 8.1%" across their stability runs; the validator's two mandated `bench:smoke` runs measured submit-50 median 8.1 → 9.6 ms (18.5% relative swing on an N=5 median; run1 min/max 7.3-9.6 vs run2 8.0-10.1 — overlapping spreads, 1.5 ms absolute). mount-50 2.6%, typing-50 1.0%, parser-medium 0.5%. No scenario approached the plan's ">50% must be investigated" bar, so this is an observation about the claim's precision, not a gate failure; submit round-trips are the noisiest of the four at N=5.
- Context: Artifacts `2026-08-31T18-43-42.842Z-current.json` vs `2026-08-31T18-44-08.929Z-current.json` (validator-executed); also a third validator run (18-45-11) with submit-50 at 9.1 ms sits between them.
- Solution: None required at Phase 1. When Phase 4 wires regression gates, the DECISION-7 headline threshold (submit-100 >10% vs reference) should be evaluated against this same-machine N=5/N=25 spread — the reference snapshot's min/max will show it; consider documenting expected submit-scenario jitter in the report output.
- Follow-up: Phase 4 design note

## [P1-rework-val] low — validator teardown stress note: SIGKILLing the pnpm wrapper orphans the vite grandchild (pre-existing e2e-inherited pattern, not reachable from a normal run)
- Problem: While stress-testing teardown, SIGKILLing only the spawned `pnpm --dir apps/web exec vite preview` wrapper left the actual `node vite.js preview` server alive and LISTENING (pnpm cannot forward SIGKILL by definition), because `stopProcess` in `tests/bench/utils/agent-browser.ts:244` (copied verbatim from `tests/e2e/utils/agent-browser.ts:578`) no-ops once its direct child is gone and never kills the process group. NOT a bench defect: unreachable from any runner-controlled path (verified the real failing-run path — a forced error inside the `try` via a PATH shim on `pnpm` — exits 1 with the `finally` closing the browser session and stopping the preview, zero orphans, port freed; verified identically after both mandated smoke runs).
- Context: Validator experiments: baseline clean; post-run-1 clean/port 44409 freed; post-run-2 clean/port 34201 freed; shim-failing run exit 1 clean. The orphan only appeared under an external SIGKILL of the wrapper process.
- Solution: None required for Phase 1. If process-group robustness is ever wanted, spawning with `detached: true` and killing the group (`process.kill(-pid)`) in `stopProcess` would cover it — a shared change for both utils, out of scope here.
- Follow-up: none (informational)

## [P2] high — main's submit lifecycle breaks back-to-back requestSubmit: the browser silently drops the next submit while main's wrapper is still tearing down

- Problem: The first full `bench:baseline` run failed inside `submit-10` (`submitLoop` never reached main's `onSubmit` within the 15s op timeout; the agent-browser eval CLI exits non-zero on a page-side rejection). Black-box tracing against the served fixture showed: every submit DOES reach the consumer `onSubmit` (~3-6ms), but main's hook wraps `formOptions.onSubmit` in its own async wrapper that only runs `clearStorage()` + `formRef.current?.reset()` AFTER the consumer promise resolves, and form-core 1.29's `_handleSubmit` only clears `isSubmitting` after THAT completes. Main's submit button is `disabled={!canSubmit || isSubmitting}`, so at the instant the fixture's loop fires the next `form.requestSubmit()` the (first) submit button is still disabled — and per the HTML spec `requestSubmit()` with a disabled implicit submitter dispatches NOTHING (no submit event, no error). One stuck waiter then times out loudly. First submit on a fresh page always works; rapid successors are the problem (12/12 submits succeed when each round trip goes through a separate agent-browser eval, i.e. with ~100ms gaps; 3+8 back-to-back in-page always stalls).
- Context: `git show main:packages/formedible/src/hooks/use-formedible.tsx` lines ~671-736 (the `onSubmit` wrapper: cross-field validation → `await formOptions.onSubmit(props)` → `clearStorage()` → unconditional `formRef.current?.reset()`; `resetOnSubmitSuccess` is destructured as `_resetOnSubmitSuccess` and never read) and ~2136+ (FormNavigation's `disabled={!canSubmit || isSubmitting || disabled || loading}`); worktree `@tanstack/form-core@1.29.0` `FormApi.js` `_handleSubmit` (`done()` runs only inside the try-block's final `batch()` after `options.onSubmit` resolves) and the derived `canSubmit` (`submissionAttempts === 0 && !isTouched && !hasOnMountError || !isValidating && !isSubmitting && isValid`). Probe evidence: submit button text frozen at "Submitting..." after one submit; exactly 1 submit event per `requestSubmit`; no page console exceptions (only main's own `console.log("handleSubmit")`/`"isLastPage")` debug output — main ships its own console.logs).
- Solution: The main fixture's `submitOnce` now captures its timing sample where the consumer `onSubmit` runs (inside the wrapper, before main's reset/teardown — so the sample stays the validation + onSubmit round trip) and resolves the loop's outer promise only after one `requestAnimationFrame` plus one macrotask, letting main's lifecycle finish before the next `requestSubmit()`. The untimed settle gap and the never-wired top-level `schema` divergence are recorded as artifact notes on every `submit-*` main record by `run-main.ts`.
- Follow-up: none (main-branch behavior, preserved and documented rather than worked around silently)

## [P2] medium — plan's "alias the pinned runtime deps" turned out half-automatic: importers inside the worktree need NO aliasing, only the fixture's own imports do

- Problem: Revised DECISION-6 says the fixture's vite config aliases main's formedible source AND the pinned runtime deps (`@tanstack/react-form`, `react`, `react-dom`, plus anything else main's src imports) to the worktree's node_modules. In practice vite 8's `resolve.tsconfigPaths` (oxc-resolver) discovers the NEAREST tsconfig per importing file, and bare imports from files INSIDE the worktree already resolve by directory walk-up into the worktree's hoisted `node_modules` (`<worktree>/packages/formedible/src` → `<worktree>/node_modules`) — the current repo's tree is never on that path. So `@tanstack/react-form` 1.29.0 (with form-core 1.29.0 + react-store 0.9.3), all radix-ui packages, cmdk, date-fns, lucide-react 0.487, sonner, react-day-picker, @dnd-kit, class-variance-authority and @base-ui are isolated WITHOUT any alias. Only the FIXTURE's own imports (which live in the current repo) needed explicit mapping: `react`, `react-dom` (identical 19.2.5 on both trees, but an unmapped import would load a SECOND React instance into the page and die on the hook-state check), the hook specifier, and the two manifest specifiers for the in-page version report. `resolve.dedupe` had to be OMITTED for the main fixture (dedupe resolves from the project root — it would pull THIS repo's react instance next to the worktree's).
- Context: `tests/bench/fixtures/main-consumer/vite.config.ts` (no `dedupe`, `tsconfigPaths: true`) vs `current-consumer/vite.config.ts` (`dedupe: ['react', 'react-dom']`); bundle-level proof in `.bench-dist/main-consumer/assets/*.js`: the embedded `@tanstack/react-form` manifest reads `version: 1.29.0` with `dependencies: {"@tanstack/react-store": "^0.9.1", "@tanstack/form-core": "1.29.0"}` and the current repo's `1.33.5` appears 0 times; React 19.2.5 appears as ONE copy with its react-dom paired-version check intact.
- Solution: Generated fixture tsconfig (from the committed template) maps exactly: `formedible-bench-main-hook` → worktree hook source, `formedible-bench-main-react-form-manifest`/`-react-manifest` → worktree manifests (the in-page isolation report), `react`/`react/*`/`react-dom`/`react-dom/*` → worktree node_modules, `@bench-scenarios` → the shared repo generators. Everything else resolves through the worktree itself.
- Follow-up: plan correction (DECISION-6's mechanism text overstates what must be aliased; the alias list is exactly the fixture-side imports)

## [P2] medium — the main fixture needs TWO tsconfigs: committed typecheck program vs generated build program (nearest-tsconfig discovery forbids one file doing both)

- Problem: vite's native `tsconfigPaths` reads the `tsconfig.json` NEAREST each importing file, so the fixture directory's `tsconfig.json` is what the build resolves through — it must contain the machine-local worktree paths (env-injected, DECISION-5) and therefore cannot be committed. But the repo's aggregate `check-types` must still typecheck the fixture's `main.tsx` on machines WITHOUT the worktree. One committed `tsconfig.json` cannot satisfy both roles (and the [P1-rework] medium finding already proved string aliases cannot replace tsconfigPaths under rolldown-vite 8).
- Context: `tests/bench/fixtures/main-consumer/`: committed `tsconfig.check.json` (the typecheck program, joined to the root `check-types` chain, with the ambient `main-hook-types.d.ts` supplying verified main-hook shapes — mirroring how `formedible-main-bridge.d.ts` covers the node parser import) + committed `tsconfig.template.json` (placeholder `__FORMEDIBLE_MAIN_WORKTREE__`) + generated gitignored `tsconfig.json` (written by `baseline-worktree.ts --setup` AND defensively re-generated by `run-main.ts` before every build, so `FORMEDIBLE_MAIN_WORKTREE` changes take effect at build time).
- Solution: Split config as above; `baseline-worktree.ts` grew `generateMainTsconfigs()` (writes both generated files) and an exported `assertBaselineReady()` shared by `--check` and `run-main.ts` (missing worktree fails with a pointer to `bench:baseline:setup`); `.gitignore` covers the generated file; the worktree path enters each generated file relative to that file's own directory (never absolute).
- Follow-up: `fixtures/minimal-consumer/` (Phase 3.2) needs no worktree mapping — a single committed tsconfig stays fine there.

## [P2] low — Phase 1 fixture configs computed the repo root one level short; `.bench-dist` actually landed at `tests/.bench-dist`

- Problem: Both committed fixture vite configs derived `repositoryRoot` via `path.resolve(fixtureDirectory, '..', '..', '..')` — three levels up from `tests/bench/fixtures/<name>` is `tests/`, not the repo root — so `build.outDir` wrote to `tests/.bench-dist/<name>` while the [P1-rework] low finding and the config comment say "repo-root `.bench-dist`". Harmless in effect (the `.bench-dist/` gitignore pattern matches at any depth, and preview reads the same outDir the build wrote), but the documented location was wrong and a stray directory accumulated under `tests/`.
- Context: vite build output line `../../tests/.bench-dist/main-consumer/index.html` (relative to `apps/web`, i.e. `/repo/tests/.bench-dist/...`) on the first main-fixture build.
- Solution: Both fixture configs now resolve four levels up; the stale `tests/.bench-dist/` was removed. New builds land at `/repo/.bench-dist/<name>`.
- Follow-up: none

## [P2] low — main's parser needs neither the dual `@/*` mapping nor an ambient registry d.ts; template and bridge reduced accordingly

- Problem: The preserved WIP `tsconfig.main.template.json` mapped `formedible-bench-main-hook`, `@/*`, `react`, and `react-dom` for the deleted jsdom in-process UI mount, and the [P1] medium finding warned Phase 2 must repeat the dual `@/*` pattern "for whichever trees the main adapter imports". Verified against main: within `packages/formedible-parser/src/lib/formedible/`, only `types.ts` has runtime (non-type) imports besides `zod` (React + zod), and it is not on the runtime path of `FormedibleParser`; main's parser has NO `registry-dependencies.d.ts` (its `src/components/` tree is real, referenced only via type-only imports tsx erases). The node worktree path therefore needs exactly ONE mapping.
- Context: `git grep "from ['\"]" main -- packages/formedible-parser/src/lib/formedible/*.ts` (only `zod` plus type-only imports reachable from `index.ts`); an in-process probe parsed the 25-field medium config via the worktree parser under `tsx --tsconfig tests/bench/tsconfig.main.json` before the runner landed.
- Solution: `tsconfig.main.template.json` reduced to the single `formedible-bench-main-parser` mapping; `formedible-main-bridge.d.ts` reduced to the parser surface (per the revised plan's Outputs), with the verified main-hook shapes MOVED to `tests/bench/fixtures/main-consumer/main-hook-types.d.ts` — the fixture is the only consumer of hook typing now that the UI baseline runs in the browser.
- Follow-up: none

## [P2] low — main's tab/array DOM hooks differ from current's; main fixture deliberately serves only the scenarios whose selectors are verified

- Problem: Current's harness drives tabs via `[data-tabs-trigger="true"]` and arrays via `[data-formedible-array-field]`/`[data-formedible-array-item]` + remove buttons matched by `aria-label` prefix — all current-repo conventions. Main's FormTabs renders plain radix `TabsTrigger`s (no data attribute), and its ArrayField renders remove `Button`s carrying `title={removeButtonLabel}` (no aria-label) with no `data-formedible-*` attributes at all. Guessing selectors now would ship unverified code paths for scenarios that Phase 3.1 registers anyway.
- Context: `git show main:packages/formedible/src/components/formedible/layout/form-tabs.tsx` and `fields/array-field.tsx` (SortableItem remove Button: `title={removeButtonLabel}`, default "Remove"; add button default label "Add Item").
- Solution: The main fixture implements the protocol operations whose drivers exist today and whose selectors are verified (`status`, `mountLoop`, `keystrokeLoop`, `submitLoop`, `switchPageLoop` via the hook API with the D11/D12 shims, `persistenceSaveLoop` with main's required-values `saveToStorage`) and serves `?scenario=<mount|typing|submit>-<N> | paged | persistent`. Tab/array operations are typed OUT of its harness (`Pick<BenchHarnessGlobal, ...>`) so a Phase 3 runner calling them fails loudly instead of driving guessed selectors.
- Follow-up: Phase 3.1 must add main-side tab (`[role="tab"]` ordered by configured tab order) and array (`title`-matched remove, "Add"-prefixed add button, container-scoped item counting) selectors together with the tabswitch/array scenarios.

## [P2] low — worktree versions block carried a dead `@tanstack/ai` entry

- Problem: The preserved `collectWorktreeEnvironment` looked up `@tanstack/ai` manifests in the worktree (root then `apps/web`), but main predates the `@tanstack/ai` family entirely — every main artifact would record `'@tanstack/ai': 'not-installed'`, pure noise in the versions block.
- Context: `git show main:packages/formedible/package.json` (peers: react-form/react/react-dom/zod only); main's lockfile has no `@tanstack/ai`.
- Solution: `WORKTREE_VERSION_MANIFESTS` in `tests/bench/lib/artifacts.ts` reduced to `@tanstack/react-form` + `react` (the runtimes main's formedible source imports). The in-page keys (`@tanstack/react-form (in-page)`, `react (in-page)`) record what the served page actually resolved.
- Follow-up: none

## [P2-val] medium — `run-main.ts` duplicates ~120 lines of runner glue from `run.ts` beyond the fixture swap

- Problem: Beyond the legitimate differences (worktree readiness, isolation assertion, divergence notes, parser import source), `tests/bench/lib/run-main.ts` copies verbatim from `tests/bench/lib/run.ts`: `parseArguments`/`failUsage` (~55 lines, only the USAGE string differs), `chromiumVersionFromUserAgent`, `withBrowserMetadata`, `runParserScenario` (~27 lines, identical loop body — only the `FormedibleParser` import source and an error-message word differ), `toRecord` (~25 lines), the `runBrowserScenario` id-regex + mount/typing verification blocks, and the `TYPING_TARGET_FIELD`/`MEDIUM_PARSER_FIELD_COUNT`/`PARSER_SCENARIO_ID` constants. The plan's Phase 3 phase-wide validation explicitly gates on "no duplication between `run.ts`/`run-main.ts` beyond the unavoidable adapter swap", so this must be deduped (shared `runner-shared.ts` for CLI parsing/record building/parser loop, parameterized by the parser constructor and implementation constant) by that point at the latest. Not a Phase 2 gate failure: Phase 2's own validation criteria are met and the plan's Phase 2 requirement says "same runner shape as `run.ts`", which the copy satisfies literally.
- Context: symbol-by-symbol comparison (`function <name>` counts 1/1 in both files; 328 vs 420 lines); Phase 2 acceptance bullets vs Phase 3 phase-wide bullet (PERF-BENCHMARK-PLAN.md line ~314).
- Solution: Deferred with an explicit deadline — extract the shared glue when Phase 3.1 touches both runners anyway (it extends both with pageswitch/tabswitch/array scenarios), so the dedup rides an already-mandatory edit of both files.
- Follow-up: Phase 3 phase-wide validation must re-check this criterion; if 3.1 does not dedup, flag as its failure.

## [P2-val] low — [P1-rework-val] cleanup suggestion not actioned: `adapter-types.ts` header still names deleted files and seven contract symbols remain consumer-less

- Problem: `tests/bench/lib/adapter-types.ts:3` still describes the contract as "shared by every implementation adapter (`adapter-current.ts` today, `adapter-main.ts` in Phase 2)" — neither file exists (both deleted in the Phase 1 rework; Phase 2 replaced them with the driver + fixture). The dead exported symbols flagged in [P1-rework-val] low still have zero consumers anywhere (`BenchAdapter`, `MountedForm`, `RootHandle`, `MountFormOptions`, `MsElapsed`, `BenchSubmitContext`, `BenchPersistenceConfig` — verified by grep; only `BenchImplementation` and the `BenchField` family are imported). The [P1-rework-val] low entry suggested Phase 2 "prune the dead symbols/refresh its header when it recreates the main-side runner, or repoint the header at the driver" — Phase 2 recreated the main-side runner (run-main.ts + fixture) without touching the header. Harmless (compiles standalone, no runtime path), but the stale header misdirects the next implementer.
- Context: `grep -rn 'adapter-current\|adapter-main' tests/bench/lib` → only the adapter-types.ts header; consumer grep for the seven symbols → 0 hits outside the contract file.
- Solution: Repoint the header at the real contract (`harness-protocol.ts` + the driver's `BenchBrowserDriver`) and delete or clearly mark dormant the unused adapter-era types in the Phase 3.1 runner edit (same file-touch window as the dedup above).
- Follow-up: Phase 3

## [P2-val] low — plan text still references deleted adapter files in Phase 1/2 Outputs (still-open item, not fixed by Phase 2)

- Problem: The [P1-rework-val] low finding asked for a plan correction; `PERF-BENCHMARK-PLAN.md` is unmodified by Phase 2 (`git diff` empty for the file). Stale spots: Phase 1 Outputs (line ~211) "Modify: ... `adapter-current.ts` (jsdom mount path removed ...)" — the file was DELETED, not modified; Phase 2 requirement 4 (line ~243) "Delete the jsdom/in-process UI mount path from `adapter-main.ts`" and Outputs (line ~252) "Modify: ... `adapter-main.ts`" — `adapter-main.ts` never existed post-rework and Phase 2 correctly created `fixtures/main-consumer/` + `run-main.ts` instead. The Phase 2 implementer also (reasonably, and disclosed in [P2] medium/low findings) modified `baseline-worktree.ts` and `tsconfig.main.template.json` even though Outputs said they "stay as committed" — the fixture-template generation requirement 2 forced it. The plan text and the implemented reality now disagree in four places.
- Context: `grep -n 'adapter-current.ts\|adapter-main.ts' PERF-BENCHMARK-PLAN.md` → lines 187/204/211/243/252 (187/204 are correct historical rework-note references; 211/243/252 are the stale ones).
- Solution: None applied by Phase 2 (not required of them per the validation dispatch). The plan-correction pass must rewrite Phase 1 Outputs ("Modify adapter-current.ts" → "Delete adapter-current.ts"), Phase 2 requirement 4/Outputs (drop the adapter-main.ts clauses, note baseline-worktree.ts/tsconfig.main.template.json were extended rather than kept as committed).
- Follow-up: plan correction (Phase 5 / final pass)

## [P2-val] low — submit-settle workaround asymmetry + validator-run variance observation (no gate impact)

- Problem: (a) Timing-integrity check of the [P2] high workaround: the sample IS clean — `elapsed` is captured inside the consumer `onSubmit` (before the untimed `nextFrame()`+macrotask settle), so no frame-delay artifact inflates main's samples. Residual asymmetry: main's `submitLoop` gives each submit a full idle frame+macrotask before the next `requestSubmit()`, while current's loop chains the next `requestSubmit()` from the settle microtask, so current's samples can absorb previous-submit teardown microtasks (persistence clear, `isSubmitting` resumption) that main's never see. Direction favors main; magnitude is sub-millisecond (current additionally disables the big cost via `resetOnSubmitSuccess: false`, whose reset main excludes by the sample boundary), against submit medians of 2-50 ms — negligible but worth one line in the Phase 4 report's caveats if submit deltas are ever read at <5% granularity. (b) Same-machine variance seen while validating: the validator-executed full `bench:baseline` (artifact `2026-08-31T19-34-53.225Z-main.json`) measured parser-medium 0.0912 ms and submit-100 50.6 ms vs the implementer's consistent 0.0333-0.0373 ms / 33.6-35.9 ms (2.4x / 1.4x) under the validator's background load — consistent with the [P1-val] low parser-position/load sensitivity note; medians still agree in order and no scenario crossed the plan's 50% investigation bar.
- Context: `tests/bench/fixtures/main-consumer/main.tsx` `submitOnce` (elapsed captured at line ~476 before the frame chain) vs `tests/bench/fixtures/current-consumer/main.tsx` `submitOnce` (resolves directly in the settle callback); artifacts listed in `tests/bench/results/runs/`.
- Solution: No code change. Phase 4's report should print the submit-settle divergence note alongside submit rows (it is already embedded in every main `submit-*` artifact record) and keep treating parser metrics as order-of-magnitude under load, per the existing variance policy (risk #5).
- Follow-up: Phase 4 design note
