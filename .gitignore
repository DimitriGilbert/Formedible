# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
*.tsbuildinfo

# Environment variables
.env
.env*.local

# IDEs and editors
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Turbo
.turbo
.nx

# Better-T-Stack
.alchemy

# Testing
coverage
.nyc_output
tests/**/.compiled
tests/e2e/.recordings

# Benchmark run artifacts (DECISION-4; the regression reference is committed)
tests/bench/results/runs/

# Benchmark fixture build output (vite build of the committed fixtures; the
# bundle embeds vendored dependency code and stays out of the bench tree)
.bench-dist/

# Generated baseline tsconfig (written by bench:baseline:setup from the
# committed template; carries the machine-local worktree path, DECISION-5)
tests/bench/tsconfig.main.json

# Misc
*.tgz
.cache
tmp
temp
old_version_for_knowledge_purpose/
